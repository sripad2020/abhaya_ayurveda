fastapi==0.115.0
uvicorn==0.30.6
gunicorn==22.0.0
jinja2==3.1.3
python-dotenv==1.0.1

# ── COMMENTED OUT — not needed without database/email ──────────
# sqlalchemy==2.0.35       # ORM — no database in this deployment
# pydantic[email]==2.9.2   # email validation — no contact form
# bleach==6.1.0            # HTML sanitiser — no user input
# slowapi==0.1.9           # rate limiter — no POST endpoints
# httpx==0.27.2            # HTTP client — no external API calls
# python-multipart==0.0.12 # form data — no form submissions
# mangum==0.17.0           # Vercel/Lambda adapter — using Render now
